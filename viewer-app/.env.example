# Backend API adresi (JWT auth, CRUD, analiz proxy)
# Geliştirme: http://localhost:5131 veya boş bırakın (Vite proxy /backend kullanır)
# Production: deploy edilen backend URL
VITE_BACKEND_URL=http://localhost:5131

# ML Prediction API adresi (soru tahmin, PDF analizi)
# Geliştirme: boş bırakın (Vite proxy /api ve /health kullanır) veya http://localhost:8000
# Production: deploy edilen ML servis URL
VITE_API_URL=
